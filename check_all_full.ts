import { db } from './server/db'; import { sql } from 'drizzle-orm'; async function run() { try { const res = await db.execute(sql.raw('SELECT table_name FROM information_schema.tables WHERE table_schema = \'public\' AND table_type = \'BASE TABLE\' ORDER BY table_name ASC')); const tables = res.rows.map(r => r.table_name); const output = []; for(const t of tables) { try { const cRes = await db.execute(sql.raw('SELECT COUNT(*) FROM public.' + t)); const count = parseInt(cRes.rows[0].count); output.push({ table: t, rows: count }); } catch(e) { } } console.log(JSON.stringify(output, null, 2)); process.exit(0); } catch(err) { console.error(err); process.exit(1); } }; run();
