# Integrated procurement acceptance ledger

This ledger describes the procurement implementation, including the completion batch documented in [the completion release guide](procurement-completion-release.md). Foundation #1398 and the combined runtime/planning release #1400 are already on main; #1404 was closed as a duplicate. The completion batch adds supplier pricing and ranking, carrier tracking, optional cost-report delivery and automatic receipt-cost recovery. Echelon operates independently; the Archon receiver and export activation are deferred. Implementation and synthetic validation do not certify production configuration or historical data. Older investigation documents describe their original baseline.

## What the code definitely does

| Requirement | Implemented path and reasoning | Evidence and practical limit |
| --- | --- | --- |
| Daily purchasing review | `purchasing-recommendation.engine.ts` computes recommendations; `purchase-recommendation-snapshot.service.ts:buildPurchaseRecommendationRunInput` captures their inputs; `automatic-rfq-draft.service.ts` and `auto-draft.job.ts` create proposed sourcing work through the existing run/command owners. Recommendations retain quantity, supplier, economics, confidence and blocking reasons. | Engine, snapshot, job and RFQ owner tests exercise the actual orchestration. No scheduler or production auto-draft setting was activated in this work. Operator setup is still required before relying on unattended daily proposals. |
| Historical demand, growth and lumpy forecasts | `purchase-planning-policy.service.ts` owns audited versioned policy. `purchasing-forecast-policy.ts:buildPurchasingForecastPolicyCohort` and `purchase-replacement-forecast.ts` preserve historical cohorts while adding uniform growth and explicit dated replacement totals. Existing demand events remain separate dated additions. | Policy, replacement, backtesting and browser tests cover neutral legacy hashes, overlapping/invalid dates, captured source totals and replay after later policy changes. Replacement totals are distributed across their inclusive date range; this is an explicit model assumption. Production forecast accuracy is not established by synthetic tests. |
| Stock targets, essential items and staged lead times | The policy DTO/editor records stock and coverage targets, essential-item policy, and RFQ/production/transit/receiving lead stages. The engine uses these inputs in target and timing calculations. | Policy and recommendation tests cover bounds and propagation. Essential targets support planning; they do not by themselves establish a stock guarantee for members. Real targets and supplier lead times remain operator inputs. |
| Timing without hiding stockouts behind open POs | `procurement.storage.ts:getReorderAnalysisData` uses `readPurchasePlanningSnapshot` and `readPurchasePlanningSupply` to read warehouse stock, closed receipt evidence and open PO supply in one read-only repeatable-read transaction. `projectPurchasePlanningSupply` removes verified physical receipts before PO synchronization; unknown history stays explicitly unresolved. `purchase-supply-timing.ts:buildPurchaseSupplyTiming` evaluates arrivals and dated demand in sequence; late, undated or inconsistent supply creates visible review evidence and blocks unattended drafting. | Tests include partial/split receipts, a close between stock and supply reads, exact posted/reversed evidence, missing historic units, supplier-confirmed dates and lumpy demand. Uncertain zero-buy rows remain visible in daily review. Legacy RFQ capture cannot authorize new automatic drafts, while exact existing RFQ replay preserves its original identity. See [receipt-planning safety proof](procurement-planning-receipt-safety.md). |
| MOQ, multiples and freight efficiency | The recommendation engine applies supplier quantity rules. `supplier-bundle.ts` and `recommendation-po-handoff.service.ts` evaluate the selected supplier basket against exact subtotal/minimum evidence. The RFQ workflow retains an explicit quantity-review reason when current supplier rules need an exception or have changed. | Engine, bundle, RFQ and real owner tests cover exact quoted-unit extension, supplier minimum rejection and unchanged-quantity rule drift. The system never pads an order to reach free freight. Unquoted freight, taxes and fees stay unknown. |
| Quotation economics | `rfq-workflow.service.ts:createRfqWorkflowService` captures final vendor quote revisions, currency, expiry, reference, price basis, frozen purchase-UOM factor and separate packaging. Explicit reviewed supplier selection remains available alongside ranked suggestions. | Exact total/per-piece/per-purchase-unit tests and real conversion/replay tests cover preserved quote economics. Catalog tiers support the proposal; the vendor's final RFQ quote is separately retained for purchase review. |
| Quantity pricing and alternate sourcing | `supplier-sourcing.ts:selectSupplierPriceTier` evaluates valid quoted tiers at the required quantity. `supplier-sourcing-selection.ts` evaluates eligible mappings using lead times and purchasing rules before deterministic ranking; the recommendation retains candidate and quote evidence. RFQ/PO owners revalidate frozen mapping and price revisions before new work. | Exact integer extension, expiry, stale policy, replay and persisted RFQ/PO tests cover the contract. Alternate suggestions remain reviewable RFQs and require explicit acceptance before PO handoff. Operator-supplied supplier priorities and quoted tiers remain required business inputs. |
| RFQ to purchase, without losing context | Migration 224 records exact immutable RFQ-line/quote-revision/PO-line links. `purchase-rfq-origin.repository.ts:readPurchaseRfqOrigins` verifies them in reverse. `PurchaseRfqOrigins.tsx` opens the RFQ inline inside `PurchaseLifecycleWorkspace.tsx`. | Actual PO conversion plus reverse-reader PostgreSQL proof; desktop/mobile navigation checks. Missing historical links remain absent; the reader never guesses by SKU, supplier or date. |
| Purchase to split/consolidated shipments, receipts and invoices | `purchase-workspace.repository.ts:createPurchaseWorkspaceRepository` reads the connected documents in a bounded repeatable-read snapshot. `PurchaseRecordInspector.tsx` keeps document detail and sibling navigation within the purchase workspace. | Workspace database and browser tests cover shared shipments, several deliveries, source identities and in-context drilldown. The shipment inspector also expands retained carrier observations without leaving the purchase; see [inbound tracking](procurement-inbound-tracking.md). |
| Production/transit value and arrival planning | `purchase-pipeline.service.ts:projectPurchasePipeline` uses exact remaining base pieces, closed receipt evidence, exact shipment lines and component source revisions. `supplier-progress.service.ts:createSupplierProgressService` records optional cumulative supplier reports with immutable history. `PurchasePipeline.tsx` displays stages, separate currencies, confirmed/estimated/unknown components and arrival source. | Real owner/repository PostgreSQL tests and desktop/mobile cases cover shared/split shipments, physical receipt before PO/AP sync, report corrections/retries and partial/conflicting cost coverage. Sent/acknowledged is not proof of production or carrier possession. Shipment ETA may refer to a port; warehouse arrival is labeled accordingly. |
| Product, packaging and freight authority | `purchase-cost-application.service.ts:recordPurchaseCostRevisions` and `shipment-cost-application.service.ts` select and freeze exact component evidence. `invoice-cost-review.service.ts` records an explicit reviewed classification without changing invoice economics. `shipment-allocation-basis.ts` verifies saved allocations against current quantity, weight, volume, PO value and effective method. | Tests exercise invoice/freight/receipt order permutations, stale allocation bases, invalid source coverage, amount preservation and exact integer residuals. Unknown components are never inferred from an unexplained invoice residual. |
| Propagation through inventory and sold-order COGS | `cost-evidence.repository.ts:recordLotCostOrigin` / `recordLotCostContribution` preserve exact origin and transformation quantities. Actual transfer/build/break/assembly/picker owners write evidence in their physical transactions. `apply-cost-revision.ts` applies one component through those descendants and the COGS owner. | Real PostgreSQL tests cover actual owners, sold-order COGS, opposite concurrent AP/transfer orderings, exact remainder layers, immutable records and late audit failure rollback. Historical missing lineage remains review-required. No claim authority was activated. |
| Physical receipt survives later cost failure | `receiving.service.ts` commits receiving before post-close reconciliation. `receipt-cost-queue.service.ts` records applied/review/retry attempts; `/api/receiving/:id/retry-costs` reuses durable command identity and requires purchasing approval capability. | Injected financial failure proves physical stock remains recorded; replay does not duplicate stock. The UI exposes attempt history and safe manual retry. `receipt-cost-recovery.worker.ts:runReceiptCostRecoveryBatch` retries eligible closed-receipt requests through the same financial owner, using bounded attempts, fenced leases and immutable recovery events. Review-required outcomes stop automation; see [receipt recovery](procurement-receipt-cost-recovery.md). |
| Solo operation and delegated controls | `purchase-order-approval.policy.ts` and identity-owned `readPurchaseApprovalActor` check current active identity, permission and configured role. Approval records its exact policy/economics evidence; send checks that evidence again. Existing no-approval solo send remains available. | Real identity + HTTP + purchasing tests cover forged roles, scoped/inactive/missing grants, revocation races, ambiguous tiers, stale policy and rollback. No invented administrator/lead hierarchy is substituted for the configured role. |
| Preserved history | Migrations 222, 223, 224 and 226 add source revisions, applications, report events, planning changes, RFQ links and supplier reports without historical backfills. Migration 227 protects purchase events and prevents deleting a purchase with retained lifecycle history. | Database immutability/replay tests and transactional owner tests. Historical corrections and manual component-protection release remain separately reviewable operations; no historical record was rewritten in production. |
| Optional downstream reporting (deferred) | The cost application transaction records its versioned source event. `cost-reporting.repository.ts:CostReportingRepository` retains destination-bound delivery records; `cost-reporting.service.ts:CostReportingService.tick` sends exact hashed evidence and accepts only a matching acknowledgement. The Archon `CostReportInbox.accept` owner retains and acknowledges it transactionally within its configured accounting entity. | Duplicate delivery and source-event conflicts cannot create another accepted record. The purchase workspace shows delivery state and acknowledgement identity. This is reporting evidence acceptance; accounting journal posting and bank settlement are separate authorities. |

## What is likely happening

No production defect frequency, inventory valuation impact or forecast accuracy is inferred from the local tests. The original fragmented navigation and component authority gaps were reproduced through code and synthetic owner fixtures; the candidate supplies the connected paths described above. Production data can still contain missing historical links, old unit evidence, incomplete quotes and unresolved costs.

## What is not proven

- Real supplier forecasts, recent stockout distortion, inventory counts and user spreadsheet results have not been reconciled against production. Planning's existing historical queries still use current catalog conversion factors where no frozen historic factor exists.
- Currency conversion authority, signed credits, tax/discount residual classification, unknown historical origins and nonrepresentable uniform-lot cost remainders remain explicit review boundaries. The candidate does not invent accounting treatment or silently absorb differences.
- The new supplier tier and ranking contracts need real supplier priorities and valid quotes. Synthetic tests do not establish that a particular supplier can fulfill a live order, and the engine never increases quantity solely to obtain a discount.
- SeaRates and ShipStation adapters require the applicable account entitlement, credentials and explicit polling activation. Provider observations do not post warehouse receipts or establish availability for sale. Supplier production reports remain operator-reported evidence.
- Archon integration is deferred and is not an Echelon acceptance gate. If resumed, its sender/receiver needs matching destination/source identity, credentials, tenant/entity binding and explicit activation. The reporting inbox preserves accepted cost evidence; no journal posting or production delivery is certified here.
- The shared graph lock has real concurrency and rollback proof. Production throughput and contention under peak warehouse load have not been benchmarked.
- Local tests use PostgreSQL 17 and installed Chrome on Windows; GitHub's configured PostgreSQL 16/Linux jobs must also pass on the exact published heads. PR checks record the current result for each release.

## Next checks

1. Verify the Echelon release against main and its deployment result. Follow [the completion release guide](procurement-completion-release.md). Keep Archon deferred and report delivery disabled; neither its PR nor its configuration blocks Echelon acceptance.
2. Apply the release manifest in order. Preserve additive schemas and all recorded history. Code rollback does not undo posted quantities or costs; after lineage-writing commands begin, retain compatible physical writers or pause those commands while correcting forward.
3. Review the first real daily recommendation run with known products: current stock, commitments, MOQ, lead stages, growth/replacement assumptions and missing evidence. Compare a representative domestic order and a long-lead imported order to the existing spreadsheet before treating the output as trusted daily input.
4. Open representative split and consolidated purchases and reconcile their original RFQ, receipt quantities, invoice components, freight allocation and arrival evidence. Confirm pickable availability through the warehouse owner, separately from the procurement pipeline.
5. Establish the historical-cost review procedure and supplier report cadence. Configure carrier tracking separately if needed, using actual account and shipment evidence. Resume external cost reporting only as a separate integration decision; those inputs are distinct from Echelon daily purchasing setup.

## Test coverage and failure modes

The release manifest contains the final command results and exact commits. Unit tests cover deterministic math, validation, domain transitions and HTTP permissions. Disposable PostgreSQL tests execute the financial, procurement, inventory and identity owners with real transactions and constraints. Browser tests exercise the real frontend with explicitly intercepted fictional API data; they prove interaction and rendering, not production connectivity.

Invalid input fails before a command commits. Conflicting versions require refreshed review. Lost responses reuse the same command key. Unsupported source evidence creates visible review states. Source/application/audit failure rolls back the financial transaction; physical receiving has its own committed boundary and durable cost recovery. History is retained through corrections and cancellation.
