import { catalogStorage } from '../server/modules/catalog'; const storage = catalogStorage; async function test() { try { const includeInactive = true; const allProducts = await storage.getAllProducts(includeInactive); const allVariants = await storage.getAllProductVariants(includeInactive); const primaryAssets = await storage.getPrimaryProductAssets(); console.log('allProducts', allProducts.length); console.log('allVariants', allVariants.length); console.log('primaryAssets', primaryAssets.length); } catch (e) { console.error('Error:', e); } } test();