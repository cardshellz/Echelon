/**
 * Route entry for Channel Inventory. The page itself lives with its
 * view-model and components under features/channel-inventory.
 */
export { default } from "@/features/channel-inventory/ChannelInventoryPage";
export { CHANNEL_INVENTORY_PATH } from "@/features/channel-inventory/ChannelInventoryPage";
